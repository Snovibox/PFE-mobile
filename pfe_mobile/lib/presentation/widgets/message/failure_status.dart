import 'package:flutter/material.dart';

class FailureStatus extends StatelessWidget{
  const FailureStatus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("error on routing"));
  }

}